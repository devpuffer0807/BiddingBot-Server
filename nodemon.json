{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["dist/*", "coverage/*", "node_modules/*"],
  "exec": "node --inspect --expose-gc --max-old-space-size=8192 -r ts-node/register/transpile-only src/index.ts",
  "env": {
    "NODE_OPTIONS": "--max-old-space-size=8192"
  }
}
